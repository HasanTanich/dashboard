import React from 'react'

const Line = (images, step, frameSize, itemWidth, animationDuration, infinite) => {
    return (
        <div className="">
            
        </div>
    )
}

export default Line